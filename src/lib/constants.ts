/**
 * Real, verified PUBLISHED + READY video used for the P2-6 live viewer playback
 * path. The landing discovery cards are mock/static (out of scope for P2-6),
 * so this single real video drives the playable "watch" path until discovery is
 * built. The backend playback endpoint remains authoritative for eligibility.
 */
export const FEATURED_VIDEO_ID = "e16fb70c-f980-4aed-85e3-fede0e1b1513";
